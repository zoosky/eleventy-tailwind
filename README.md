# eleventy-tailwind

A demo project

To run

```bash
    npm install
    
    npx eleventy --serve
    # same as
    npx @11ty/eleventy --input=. --output=_site
´´´


To run tailwind, use

    npx tailwindcss -i ./tailwind.css -o ./_site/css/styles.css --watch